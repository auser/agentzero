---
title: Overview
description: "Self-growing AI agent infrastructure. Describe tools and agents in plain English — the system assembles, remembers, and gets smarter over time."
---

import { Card, CardGrid } from '@astrojs/starlight/components';

AgentZero is **self-growing AI agent infrastructure** built entirely in Rust. Describe what you need in plain English — the system creates tools, defines agents, assembles swarms, and **remembers what works**. Every session makes the next one smarter.

:::note
AgentZero is under active development. The architecture is stable but individual features may change between releases.
:::

## Why AgentZero

- **Self-growing.** Every tool created, agent defined, and recipe learned persists encrypted across sessions. The system compounds — tools from week 1 accelerate goals in week 4.
- **Natural language tools.** Describe a missing tool in conversation. The system creates it (shell, HTTP, LLM, or composite strategy), registers it immediately, and persists it forever. Export and share tools between instances.
- **NL agent definitions.** Say "an agent that reviews my PRs daily" and the system derives name, system prompt, keywords, allowed tools, and schedule automatically. Agents are stored encrypted and auto-routed to by keywords.
- **Autonomous swarms.** Give a goal, get a parallel agent DAG. Each node gets only the tools it needs via per-node tool hints. Sandboxed execution with conflict detection and dead agent recovery.
- **Deploy anywhere.** A single native binary runs on Linux, macOS, and Windows — 8 platform targets, Docker multi-arch. 4.5 MB embedded binary.
- **Secure by default.** Encrypted storage, allowlists, policy gates, audit logging, secret redaction, and emergency stop. Fail-closed from day one.

## Features

<CardGrid>
  <Card title="Self-Growing" icon="star">
    Tools, agents, and recipes persist encrypted. The system learns what works and reuses it. Month 2 is smarter than month 1.
  </Card>
  <Card title="NL Tools & Agents" icon="rocket">
    Describe tools and agents in plain English. Created mid-session, persisted forever, searchable and shareable.
  </Card>
  <Card title="Autonomous Swarms" icon="puzzle">
    Give a goal. The system decomposes it into a parallel agent DAG with per-node tool filtering and sandboxed execution.
  </Card>
  <Card title="Catalog Learning" icon="random">
    Successful tool combos are recorded as recipes. Future goals matching similar patterns get the right tools instantly.
  </Card>
  <Card title="Small & Fast" icon="approve-check">
    4.5 MB embedded binary. Millisecond cold starts. Async Tokio runtime with streaming SSE and WebSocket output.
  </Card>
  <Card title="Secure by Default" icon="setting">
    Encrypted storage, deny-by-default tools, filesystem scoping, shell allowlists, secret redaction, audit trails.
  </Card>
</CardGrid>

## At a Glance

| | |
|---|---|
| **Language** | 100% Rust |
| **Crates** | 16 workspace crates |
| **Binary Size** | 4.5 MB embedded / 18 MB full |
| **Platform Targets** | 8 (Linux x86/ARM/musl, macOS Intel/ARM, Windows) |
| **Client SDKs** | Python, TypeScript, Swift, Kotlin |
| **CLI Commands** | 38+ subcommands |
| **Providers** | 37+ (OpenAI, Anthropic, Google, Ollama, local llama.cpp, and more) |
| **Memory** | SQLite (default, encrypted), Turso/libsql (optional) |
| **Tools** | 58+ built-in (file I/O, shell, networking, browser, delegation, memory, git, cron, hardware, autopilot, and more) |
| **MCP** | First-class Model Context Protocol — each MCP tool registered individually with real schema |
| **Channels** | 25+ (Telegram, Discord, Slack, Matrix, Email, WhatsApp, SMS, and more) |
| **Plugins** | WASM sandbox with integrity verification |
| **Security** | Encrypted storage, allowlists, OTP gates, audit trail, secret redaction, estop |
| **Multi-Agent** | Delegation, swarm coordination, pipelines, event bus, persistent named agents |
| **Autopilot** | Autonomous agent loop — proposals, cap gates, missions, triggers, reaction matrices |
| **API Docs** | Interactive Scalar docs at /docs on every gateway deployment |
| **License** | MIT / Apache-2.0 |

## Quick Start

```bash
# Build from source
cargo build -p agentzero --release

# Run interactive onboarding
cargo run -p agentzero -- onboard --interactive

# Set your API key
export OPENAI_API_KEY="sk-..."

# Send a message
cargo run -p agentzero -- agent -m "hello"

# Check system health
cargo run -p agentzero -- doctor models
cargo run -p agentzero -- status
```

See the full [Installation](/agentzero/installation/) guide and [Quick Start](/agentzero/quickstart/) for details.

## Project Links

- [GitHub Repository](https://github.com/auser/agentzero)
- [CLI Command Reference](/agentzero/reference/commands/)
- [Configuration Reference](/agentzero/config/reference/)
- [Architecture](/agentzero/architecture/)
- [Threat Model](/agentzero/security/threat-model/)
