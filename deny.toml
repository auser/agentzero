[advisories]
# Only fail on unmaintained/unsound crates that are direct workspace dependencies.
# Transitive deps (ratatui→paste, libsql→rustls-pemfile, ratatui→lru) are outside
# our direct control and tracked below for upgrade when upstream releases fixes.
unmaintained = "workspace"
unsound = "workspace"
ignore = [
    # paste 1.0.15 via ratatui — awaiting ratatui upgrade
    "RUSTSEC-2024-0436",
    # fxhash 0.2.1 via inquire — awaiting inquire upgrade
    "RUSTSEC-2025-0057",
    # rustls-pemfile 2.2.0 via libsql — awaiting libsql upgrade
    "RUSTSEC-2025-0134",
    # bincode 1.3.3 via libsql — awaiting libsql upgrade
    "RUSTSEC-2025-0141",
    # lru 0.12.5 IterMut unsoundness via ratatui — awaiting ratatui upgrade
    "RUSTSEC-2026-0002",
    # rustls CRL distribution point matching — awaiting rustls upgrade
    "RUSTSEC-2026-0049",
]
